.hlx/*
coverage/*
logs/*
node_modules/*

helix-importer-ui
.DS_Store
*.bak
.idea

# secrets — never commit the DA IMS token
.env
.env.*

# local QA harness (playwright scratch, not shipped)
qa/

# stardust migration scratch — bot-clearance session (sensitive cookies) + run logs
stardust/px-clearance.json
stardust/*.log

# Ensure these files are tracked
!CLAUDE.md
!AGENTS.md
